name: Clean AI Health APK

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/flutter-apk.yml'

permissions:
  contents: read

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout clean rebuild source
        uses: actions/checkout@v4
        with:
          ref: clean-rebuild

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter stable
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Generate Android project
        run: |
          cp clean_app/pubspec.yaml /tmp/pubspec.yaml
          cp clean_app/lib/main.dart /tmp/main.dart
          cd clean_app
          flutter create --platforms=android --org com.aimyhealthmanager --project-name ai_health_assistant .
          cp /tmp/pubspec.yaml pubspec.yaml
          cp /tmp/main.dart lib/main.dart
          rm -f test/widget_test.dart

      - name: Configure Android app
        run: python3 scripts/configure_clean_android.py

      - name: Install dependencies
        working-directory: clean_app
        run: flutter pub get

      - name: Analyze source
        working-directory: clean_app
        run: flutter analyze lib --no-fatal-infos --no-fatal-warnings

      - name: Build release APK
        working-directory: clean_app
        run: flutter build apk --release

      - name: Prepare artifact
        run: cp clean_app/build/app/outputs/flutter-apk/app-release.apk AI_건강비서_v1.1.0.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ai-health-assistant-v1-1-0-apk
          path: AI_건강비서_v1.1.0.apk
          if-no-files-found: error
          retention-days: 30
